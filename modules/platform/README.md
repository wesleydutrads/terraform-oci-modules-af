# platform

Installs Kubernetes platform components for OKE: Gateway API CRDs, cert-manager, external-dns, Istio ambient mode, public ingress gateway, and optional wildcard certificate resources.

The caller must provide domain, ACME email, and kubeconfig settings.
