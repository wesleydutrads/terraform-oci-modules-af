# observability

Installs Kiali, kube-prometheus-stack, and optional Jaeger, then creates HTTPRoutes for each dashboard through a shared Gateway.

The caller supplies hostnames and the shared access token.
