# OKE Platform Example

Example root module showing how to compose the Always Free modules.

This example intentionally uses placeholder values. Copy it into a separate environment repository and provide real values through `terraform.tfvars`.
